A continuación, encontrarás una lista de oportunidades para mejorar la accesibilidad de Vinilos. Cada elemento corresponde a un área descrita en la captura de pantalla adjunta.

Etiqueta del elemento
[0,135][168,303]
Es posible que este elemento no tenga una etiqueta que pueda leer un lector de pantalla.

Contraste del texto
com.misw.vinilos:id/btnSave
¿Por qué no aumentas la relación de contraste entre el color de fondo y de primer plano del texto de este elemento?
La relación de contraste del texto del elemento es 3.95. Esta cifra se basa en un color de primer plano estimado de #FFFFFF y un color de fondo estimado de #7B6CF6. Te recomendamos que aumentes la relación de contraste del texto de ese elemento a 4.50 o a un valor superior.

Descripciones de elementos
com.misw.vinilos:id/text_input_end_icon
Hay varios elementos con la misma descripción.
El texto hablado de este elemento en el que se puede hacer clic: "no marcado, Mostrar menú desplegable" es idéntico al de otros 2 elementos.

Ajuste de texto
com.misw.vinilos:id/btnSave
Esta TextView tiene una altura fija y un texto ajustable.
Prueba a modificar los LayoutParams para permitir que el texto se amplíe.

Ajuste de texto
com.misw.vinilos:id/toolbar
Este ViewGroup tiene una altura fija y contiene una TextView con texto ajustable.
Prueba a modificar los LayoutParams para permitir que el texto se amplíe.

Ajuste de texto
com.misw.vinilos:id/flCoverPicker
Este ViewGroup tiene un tamaño fijo y contiene una TextView con texto ajustable.
Prueba a modificar los LayoutParams para permitir que el texto se amplíe.

Texto no expuesto
com.misw.vinilos:id/etAlbumName
Posible texto detectado: Nombre del Ábum
Comprueba que la etiqueta de accesibilidad de este elemento incluya su texto de forma visible.

